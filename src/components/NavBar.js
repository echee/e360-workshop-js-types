// @flow
import React from 'react';

const NavBar = () => {
  return (
    <nav
      className="navbar is-dark"
      role="navigation"
      aria-label="main navigation"
    />
  );
};

export default NavBar;
